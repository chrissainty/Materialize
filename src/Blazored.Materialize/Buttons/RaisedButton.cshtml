@inherits ButtonBase

<a class="waves-effect waves-light btn @(IsSmall ? "btn-small" : "") @(IsLarge ? "btn-large" : "")" disabled="@Disabled">@ChildContent</a>

@functions 
{
    [Parameter]
    private RenderFragment ChildContent { get; set; }
}